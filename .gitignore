# 抓取原始响应缓存（体积大，可随时删除后重跑）
tools/out/*cache*/

# index.html 由 CI（GitHub Pages）从 src/ 构建产出，不入库
index.html

# 临时诊断产物（tools/check_data.js 等脚本自动生成，可随时重跑）
tools/out/_*

# WorkBuddy 项目数据，不入库
.workbuddy/

# Qoder 编辑器本地数据，不入库
.qoder/

# 其他机器生成的 Git 辅助脚本，不入库
tools/_gitrun.js
