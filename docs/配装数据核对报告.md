---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: b6cfada211841cacada780bcf9558b1e_0accda0db2ac11f18368525400248c00
    ReservedCode1: ze+DVgCSrqMxDJXAKNi87Y3xBC4diXZI+Jivzhf+FMZYEECTtm2BnIf/CRG5B9TTMCBUDnTMCa5sjPIEZ3BZkJsPYix5I6cGgsl/qejMJeNv8BSo5wei1gAOYQ1Kc8fliDhzq2VT9GETaSp88fwFBsB9+7Z6/mU/gf921X/BfffCWtpMUXB2A3Js7sE=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: b6cfada211841cacada780bcf9558b1e_0accda0db2ac11f18368525400248c00
    ReservedCode2: ze+DVgCSrqMxDJXAKNi87Y3xBC4diXZI+Jivzhf+FMZYEECTtm2BnIf/CRG5B9TTMCBUDnTMCa5sjPIEZ3BZkJsPYix5I6cGgsl/qejMJeNv8BSo5wei1gAOYQ1Kc8fliDhzq2VT9GETaSp88fwFBsB9+7Z6/mU/gf921X/BfffCWtpMUXB2A3Js7sE=
---

# genshin-artifact-lock 角色配装数据核对报告

- 核对时间：2026-09-17
- 核对对象：`src/data.js` 内置数据（125 角色 / 352 配装组）
- 比对基准：
  - `tools/out/wiki_builds.json`（观测枢 wiki 角色词条「推荐装备 → 圣遗物推荐」结构化快照，抓取于 2026-09-14 23:46）
  - `tools/out/guides.json`（各角色作者攻略链接池，同为 125 角色）
  - 每组配装的 `reason` 原文（wiki 页面上抓下的原始推荐描述文本），用于反查解析完整性
- 核对方式：Node vm 沙箱加载 `data.js` 导出 `RAW_CHARS` / `buildDefaultCharacters()` 结果，与两份快照逐字段比对，并对 `reason` 文本做词条级反向校验。脚本见 temp 目录（`dump_raw.js`、`compare.py`、`text_vs_struct2.py`）。

## 一、总体结论

| 层级 | 结论 |
| --- | --- |
| 快照 → 内置数据 | **一致**：352 组全字段 0 差异，生成管线忠实无丢数据 |
| wiki 原文 → 快照 | **有损**：7 处副词条被静默丢弃（别名未覆盖） |
| 派生字段 subRules | **不可信**：122 组必选标记被运行时清零，82.4% 的组最终没有必选词条 |
| 配装本体（套装 / 主词条 / 副词条 / 定位） | **大体正确**，存在 7 处词条缺失、5 组定位为空、若干条件词条被当常规推荐 |

即：当前保存的配装攻略在「套装选择 + 主词条」层面与 wiki 一致，可以直接使用；但在「副词条完整性」「必选标记」「条件性推荐」三处存在确定性缺陷，需修复后才算完全正确。

## 二、快照 ↔ 内置数据：0 差异

| 核对项 | 结果 |
| --- | --- |
| 配装组总数 | wiki 352 = data 352 |
| 角色数 | 125 = 125，无缺失 |
| 属性集合不一致 | 0 |
| 属性顺序不一致 | 0 |
| 功能定位不一致 | 0 |
| 来源链接异常 | 0 |

说明：`apply_wiki_builds.py` 的回写是逐字段无损的，此前担心的「解析层丢弃」问题不发生在回写环节，而发生在更上游的 wiki 抓取解析环节。

## 三、wiki 原文 → 快照：7 处词条丢失（确定性缺陷）

以每组 `reason` 原文为基准反查结构化字段，共 7 处「原文写了、结构化没有」：

| 角色 | 配装 | 位置 | 原文 | 丢失词条 | 当前结构化值 |
| --- | --- | --- | --- | --- | --- |
| 多莉 | #0 海染砗磲 | 副词条 | 生命值/元素充能效率/**精通** | em | hpP, er |
| 多莉 | #2 被怜爱的少女 | 副词条 | 生命值/元素充能效率/**精通** | em | hpP, er |
| 卡齐娜 | #0 烬城勇者绘卷 | 副词条 | **暴击**/暴击伤害/元素充能效率/防御力 | cr | cd, er, defP |
| 卡齐娜 | #1 黄金剧团 | 副词条 | **暴击**/暴击伤害/元素充能效率/防御力 | cr | cd, er, defP |
| 瑶瑶 | #0 深林的记忆 | 副词条 | 元素精通/元素充能效率/**生命加成**/攻击力 | hpP | em, er, atkP |
| 瑶瑶 | #1 千岩牢固 | 副词条 | 同上 | hpP | em, er, atkP |
| 瑶瑶 | #2 海染砗磲+被怜爱的少女 | 副词条 | 同上 | hpP | em, er, atkP |

- 根因：解析层别名表（STAT_ALIAS）未覆盖 wiki 的简写/异写——`精通`、`暴击`、`生命加成` 三种写法未命中白名单，未识别片段被静默丢弃。
- 注意 `生命值百分比`（主词条写法）已被正确识别为 hpP，只有副词条里的 `生命加成` 漏了，说明是别名枚举不全而非逻辑错误。

## 四、结构化字段的其他质量问题

1. **5 组功能定位（roles）为空**：闲云 #1 昔日宗室之仪、云堇 #0 华馆梦醒形骸记、云堇 #1 昔日宗室之仪、伊涅芙 #2 千岩牢固、莉奈娅 #1 华馆梦醒形骸记。这 5 条的 reason 文本都存在（描述套装效果），但未推导出 roles，会连带造成 subRules 推导失真。
2. **条件性词条被当作常规推荐**：wiki 原文中带括号的条件写法被无差别收录为常规词条，典型如
   - 欧洛伦 #0：`副词条：充能效率/暴击率（仅西风猎弓）` → cr 被当常规词条；该行 `空之杯：不强求` 也被解析成空数组；
   - 辛焱：`攻击力（六命可选防御力）` → defP 被当常规沙漏/副词条；
   - 久岐忍（携带西风剑时）、迪奥娜 / 雅珂达（西风猎弓）、希诺宁（西风剑推荐）、爱诺（触发西风大剑特效）→ 均把西风系条件暴击率当常规词条。
3. **reason 分隔符不统一**：存在 `/`、空格分隔（阿贝多、刻晴的副词条为空格分隔）、`或者`（柯莱）、以及「（输出）/（辅助）」双版本混写（蓝砚）。当前解析靠启发式兜住，是上述丢失问题的温床。

## 五、派生字段 subRules（★必选）不可信

352 组统计：

| 指标 | 数值 |
| --- | --- |
| source = heuristic | 318（90.3%） |
| source = unset（无规则） | 29（8.2%） |
| source = manual（人工校准） | 5（1.4%） |
| required 写了但被收紧逻辑清零（死字段） | 122（34.7%） |
| 展开后 ★必选为空的组 | 290（82.4%） |
| required 候选分布 | em 129 / er 60 / hpP 26 / defP 2 |
| equal 组分布 | cr&cd 263（唯一形态） |

结论：★必选标记并非来自 wiki（wiki 无此字段），而是由 `role_infer.py` 按描述前缀关键词启发式推导，再被 `subIdsToSubs()` 的「前缀块收紧」逻辑清零，最终 82.4% 的组没有任何必选词条。若锁定规则依赖 ★，则实际等价于没有必选约束，必须修复或废弃。

## 六、来源与攻略链接

- 125 角色均为 3 条来源，首条全部为观测枢词条（`baike.mihoyo.com`），无重复链接，作者名与 `guides.json` 完全一致，无越界来源。
- 13 个角色在 `guides.json` 中无白名单（whitelisted）作者攻略：烟绯、迪希雅、流浪者、提纳里、安柏、丽莎、埃洛伊，以及旅行者·风 / 岩 / 雷 / 草 / 水 / 火。
- 作者攻略正文（图文攻略）本项目从未读取，因此「攻略」只能核到链接与作者层面，无法核对具体数值结论。
- 时效性：快照抓取于 2026-09-14，`data.js` 生成于 2026-09-17，快照已滞后 3 天；若 wiki 期间有修订，当前数据不会自动跟进。

## 七、修复建议（按优先级）

| 优先级 | 动作 |
| --- | --- |
| P0 | 补全 STAT_ALIAS 别名：`精通`→em、`暴击`→cr、`生命加成`→hpP，并改为「未识别片段上报 + 非空校验」，禁止静默丢弃 |
| P0 | 移除 required 死字段收紧逻辑或改为显式 schema（`{core, required, optional}`），让 ★必选重新承载语义 |
| P1 | 处理括号条件词条：`（六命可选…）`、`（携带西风…时）`、`仅西风猎弓` 标记为 optional/conditional，不并入常规推荐 |
| P1 | 补齐 5 组缺失 roles，并回填至 `wiki_builds.json` 后再生成 `data.js` |
| P2 | 统一 reason 解析分词规则（`/`、空格、`或者`、`（输出）/（辅助）` 多版本），并建立解析回归测试 |
| P2 | 重新抓取 wiki 快照，消除 3 天滞后 |

## 附：可复现的核对口径

1. `node dump_raw.js`：沙箱加载 `data.js`，导出 `RAW_CHARS` / 展开后角色数据 / `CHAR_META`。
2. `compare.py`：快照 ↔ 内置数据逐字段比对（配装组数、套装集合、三部位主词条、副词条集合与顺序、roles、来源链接）。
3. `text_vs_struct2.py`：以 `reason` 原文反查结构化字段，双向输出「文本有结构没有」「结构有文本没提」「未识别片段」三类清单。
*（内容由AI生成，仅供参考）*
